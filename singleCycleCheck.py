# single cycle is present if we jump correspondingly according to the numbers
# in the list and if we come back at the first element of the list after having 
# each element in the list traversed once.
# eg:array=[2,3,1,-4,-4,2] HAS A SINGLE singleCycle
#    array=[1,-1,1,-1]     DOESN'T HAVE A SINGLE singleCycle

def singleCycleCheck(array):
    initialOne=array[0]
    

if "__init__"=="__main__":
    array=[2,3,1,-4,-4,2]
    singleCycleCheck(array)